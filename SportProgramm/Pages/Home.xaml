<Page x:Class="SportProgramm.Pages.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SportProgramm.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="1014" d:DesignWidth="1920"
      Title="Home">

    <Grid>
        <DockPanel Margin="0 50 0 0">
            <DataGrid x:Name="TournamentsGrid" DockPanel.Dock="Top" AutoGenerateColumns="False"
                 HeadersVisibility="None" Background="Transparent" BorderThickness="0"
                 RowHeight="100" IsReadOnly="True">
                <DataGrid.Resources>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource TournamentCardStyle}"
>
                                    <Grid Margin="10">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Text="{Binding Name}" 
                                              FontSize="16" FontWeight="Bold"/>
                                        <TextBlock Grid.Row="1" Text="{Binding Place}" 
                                              FontSize="12" Foreground="Gray"/>
                                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                                            <TextBlock Text="Лучший результат: " FontSize="12"/>
                                            <TextBlock Text="{Binding Score}" FontSize="12" FontWeight="Bold"/>
                                        </StackPanel>
                                        <TextBlock Grid.Row="3" Text="{Binding Date, StringFormat='dd.MM.yyyy'}" 
                                              FontSize="10" Foreground="DarkGray"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</Page>
